{
  "name": "gtec-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@gtec/web && npm run build --workspace=@gtec/server",
    "lint": "npm run lint --workspace=@gtec/web",
    "typecheck": "npm run typecheck --workspace=@gtec/web && npm run build --workspace=@gtec/server",
    "test": "npm run test --workspace=@gtec/server",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "@types/pg": "^8.11.6",
    "tsx": "^4.23.11",
    "typescript": "^5.5.3"
  }
}
