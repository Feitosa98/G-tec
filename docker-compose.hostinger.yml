services:
  web:
    build:
      context: https://github.com/Feitosa98/G-tec.git#main
      args:
        VITE_BASE_PATH: /
    restart: unless-stopped
    ports:
      - "8080:80"
    security_opt:
      - no-new-privileges:true
